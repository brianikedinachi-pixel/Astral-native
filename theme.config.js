/** @type {const} */
const themeColors = {
  primary: { light: '#00e5ff', dark: '#00e5ff' }, // Cyan accent
  background: { light: '#05080f', dark: '#05080f' }, // Dark navy
  surface: { light: '#0d1117', dark: '#0d1117' }, // Slightly lighter
  foreground: { light: '#e8f4ff', dark: '#e8f4ff' }, // Light blue-white
  muted: { light: '#5a7a9a', dark: '#5a7a9a' }, // Muted blue
  border: { light: 'rgba(0, 229, 255, 0.14)', dark: 'rgba(0, 229, 255, 0.14)' }, // Cyan with opacity
  success: { light: '#22C55E', dark: '#4ADE80' },
  warning: { light: '#F59E0B', dark: '#FBBF24' },
  error: { light: '#ff4d6d', dark: '#ff4d6d' }, // Red for errors
};

module.exports = { themeColors };
